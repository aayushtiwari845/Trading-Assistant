FROM python:3.11-slim

WORKDIR /app
COPY pyproject.toml README.md /app/
COPY src /app/src
COPY assets /app/assets
RUN pip install --upgrade pip && pip install -e .

CMD ["uvicorn", "trading_research.api.main:app", "--host", "0.0.0.0", "--port", "8000"]

